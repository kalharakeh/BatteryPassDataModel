@{
    ViewData["Title"] = "Create Passport";
}
<h1 class="h3">Create Passport</h1>
<div class="alert alert-warning mt-3">Create form migration is in progress for the Pivot-style rewrite branch.</div>
<p class="text-secondary">Use the legacy app reference under <code>web-next-legacy</code> for the full editor until this screen is fully migrated.</p>
